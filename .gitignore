# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/
.env
*.egg

# Node
ui/node_modules/
ui/dist/
# but keep a placeholder so the wheel's force-include of ui/dist always resolves
# (a fresh `pip install -e` / CI checkout has no built UI). Releases build over it.
!ui/dist/.gitkeep
ui/.vite/

# Bundled UI copied into the package at build time
vapviz/_static/

# Logs
*.log
/logs/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Temp
/C:/Temp/

# Runtime data — local SQLite stores and server logs
vapviz.db
vapviz.db-shm
vapviz.db-wal
*.db
*.db-shm
*.db-wal
.vap_server.out
.vap_server.err
